# Сергей Смирнов — сайт-визитка

Статический лендинг без сборки и зависимостей: `index.html` и `styles.css`.
Откройте `index.html` в браузере или разместите репозиторий через GitHub Pages
(источник: `main`, корень `/`).
